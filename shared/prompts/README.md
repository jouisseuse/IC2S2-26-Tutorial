# Prompts

Prompts are stored separately from game logic.

Prompt folders now include simple system and task prompts for:

- Public goods contribution decisions
- Social learning estimates and revisions
- Coordination choices

Each decision prompt asks for structured JSON only, so model responses can be parsed into the shared action schema.

