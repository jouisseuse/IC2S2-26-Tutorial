# Agent counts define the composition of a group.
# Values should sum to the game group_size for fixed-size games.
agents:
  human: 2
  llm: 2
  scripted: 0
  random: 0

defaults:
  actor_type_order:
    - human
    - llm
    - scripted
    - random
  allow_mixed_groups: true
